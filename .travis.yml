language: python
services:
  - docker
python:
  - "2.7"
install:
  - make deps
script:
  - make test
  - make lint
after_success:
  - make docker_build
  - make docker_push

deploy:
  provider: heroku
  app: ancient-sea-62343
  api_key: ${HEROKU_API_KEY}
